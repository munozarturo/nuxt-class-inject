// @ts-check

(() => {
  const w = window;
  const de = document.documentElement;
  const ls = window.localStorage;

  addClassName("dark-mode");

  /** @param {string} className */
  function addClassName(className) {
    if (de.classList) {
      de.classList.add(className);
    } else {
      de.className += " " + className;
    }
  }
})();
